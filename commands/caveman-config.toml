description = "Show or update Caveman config"
prompt = "Run `node src/commands/caveman-config.js {{args}}`. Supported examples: `show`, `set targetModel claude-opus-4-8`, `set injection.strategy adaptive`, `set compression.defaultStrategy hybrid`."
