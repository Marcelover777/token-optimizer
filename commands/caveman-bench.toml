description = "Run Caveman offline/online benchmark report (token-cost optimizer, Opus 4.8)"
prompt = "Run `node src/commands/caveman-bench.js {{args}}` from the repo root. Use `--offline` when no ANTHROPIC_API_KEY exists. Use `--online --model claude-opus-4-8 --max-spend 1` for a budgeted real-API run. Use `--report` to write the dated JSON/MD report under evals/reports/."
